# Python dependencies for the Sales Tax Nexus Tracker.
# Kept minimal and mainstream on purpose (see PROJECT_SPEC.md section 3).
#
# The core of the app -- ledger, storage, nexus engine, CSV import -- uses only
# the Python standard library (sqlite3, json, csv, datetime).
#
# Install into a project-local virtual environment:
#   python3 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt

# Session 4 & 7 -- the local web app the user opens in a browser.
Flask>=3.0,<4

# Session 6 (Shopify connection) will add, when built:
#   requests          -- pulling orders from the Shopify Admin API
#   cryptography      -- encrypting the per-store token at rest
